/**
 * Classe base di gestione reazione all'azione all'interno di un reducer
 */
export default class {

    constructor() {
        
    }

    /**
     * Reazione all'azione
     * @param state Stato applicazione
     * @param action azione
     * @return Nuovo stato 
     */
    doAction(state, action) {
    }

}